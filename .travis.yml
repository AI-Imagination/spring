language: cpp
cache:
  - ccache
sudo: required
dist: trusty

os:
  - linux

addons:
  apt:
    packages:
      - g++-5.4
      - git
      - clang-format-3.8
      - ccache

script:
  /bin/bash ./tests.sh

notifications:
  email:
    on_success: change
    on_failure: always
