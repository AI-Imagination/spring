language: cpp
cache:
  - ccache
sudo: required
dist: trusty

os:
  - linux

addons:
  apt:
    packages:
      - gcc-5
      - git
      - ccache

script:
  /bin/bash ./tests.sh

notifications:
  email:
    on_success: change
    on_failure: always
